PRINT "Printing FizzBuzz from 0 to 100"

FOR i = 1 TO 100
    IF i % 15 == 0 THEN
        PRINT "FizzBuzz"
    ELIF i % 5 == 0 THEN
        PRINT "Buzz"
    ELIF i % 3 == 0 THEN
        PRINT "Fizz"
    ELSE
        PRINT i
    ENDIF
NEXT
